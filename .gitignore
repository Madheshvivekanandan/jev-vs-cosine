# Secrets: never commit
.env
.env.*
!.env.example

# Local environment and tool caches
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Downloaded dataset (re-creatable from the pinned commit)
data/

# Local caches, except Jev's answers: each one spends free daily quota to recreate.
.cache/*
!.cache/jev_decisions.jsonl

# Claude Code per-user settings
.claude/settings.local.json
